apiVersion: apps/v1
kind: Deployment
metadata:
  name: cat-webapp
  labels:
    {{- include "catwebapp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "catwebapp.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "catwebapp.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
